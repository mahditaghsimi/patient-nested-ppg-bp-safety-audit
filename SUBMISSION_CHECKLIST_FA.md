# چک‌لیست پیش از ارسال مقاله

- اجرای کامل `python main.py` روی محیط نهایی با موفقیت تمام شود.
- `artifacts/FINAL_COMPLETION_AUDIT.json` و `artifacts/claim_audit.json` وضعیت PASS داشته باشند.
- baseline رسمی source-training پس از اصلاح دوباره محاسبه شده باشد.
- در متن روشن باشد که external fixed prior با nested selected procedure یکسان نیست.
- نام نویسندگان، affiliation، ایمیل و corresponding author جایگزین Anonymous شوند.
- statement اخلاق پژوهش، IRB/exemption و مجوز استفاده از داده با اطلاعات واقعی مؤسسه تکمیل شود.
- DOI، جلد، صفحات و اطلاعات همهٔ منابع بررسی شوند.
- PDF اصلی و ضمیمه از نظر layout، فونت، شکل‌ها و سقف صفحات بصری بررسی شوند.
- مجوز بازتوزیع داده‌ها و checkpointها تأیید شود؛ در غیر این صورت فقط script دانلود و manifest منتشر شود.
- نتیجه به‌عنوان retrospective safety/methodology audit ارائه شود، نه دستگاه تشخیصی یا مدل clinically interchangeable.

