# Claim policy

Historical numeric claims are intentionally not bundled as authoritative values.
`scripts/verify_claims.py` compares the manuscript generated in the current run
with the artifacts from that same run and writes `artifacts/claim_audit.json`.

